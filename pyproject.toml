[project]
name = "finkit-dev"
version = "0.2.0"
description = "Development dependencies for fin-kit"
requires-python = ">=3.13"
dependencies = [
    "conan>=2.0",
    "pre-commit>=3.0",
]

[dependency-groups]
dev = []
